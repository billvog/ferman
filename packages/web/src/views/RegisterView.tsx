import { Box } from "@chakra-ui/layout";
import React from "react";

interface RegisterViewProps {}

export const RegisterView: React.FC<RegisterViewProps> = ({}) => {
  return <Box>this is the register view!</Box>;
};
