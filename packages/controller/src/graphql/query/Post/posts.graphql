query Posts(
  $limit: Int!
  $skip: Int
  $userId: Int
  $query: String
  $feedMode: Boolean
  $likedBy: Int
) {
  posts(
    limit: $limit
    skip: $skip
    userId: $userId
    query: $query
    feedMode: $feedMode
    likedBy: $likedBy
  ) {
    ...PaginatedPosts
  }
}
