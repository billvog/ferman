query Posts(
  $limit: Int!
  $skip: Int
  $userId: Int
  $query: String
  $feedMode: Boolean
) {
  posts(
    limit: $limit
    skip: $skip
    userId: $userId
    query: $query
    feedMode: $feedMode
  ) {
    hasMore
    executionTime
    count
    posts {
      ...FullPost
    }
  }
}
