mutation UpdateProfile($options: ProfileInput!) {
  updateProfile(options: $options) {
    error {
      ...FieldError
    }
    user {
      ...FullUser
    }
    profile {
      ...FullProfile
    }
  }
}
