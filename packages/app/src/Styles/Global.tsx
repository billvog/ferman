import { StyleSheet } from "react-native";

export const MAIN_DARK_BLUE = "#334963";

export const GlobalStyles = StyleSheet.create({
  Container: {
    paddingTop: 10,
    paddingBottom: 10,
  },
});
